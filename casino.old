  // Player Object  

  function Player(cards,money_bet, roulette_bet,money){
      this.cards = [],
      this.money_bet ='',
      this.roulette_bet=''
      this.money=money
  }

  const dealer={
    dealer_cards : []
  }
  
  // Black Jack Functions
  const deckOfCards = {
    ace:4,
    king:4,
    queen:4,
    jack:4,
    ten:4,
    nine:4,
    eight:4,
    seven:4,
    six:4,
    five:4,
    four:4,
    three:4,
    two:4
  };


const cardValues = {
  ace:[1,11],
  king:10,
  queen:10,
  jack:10,
  ten:10,
  nine:9,
  eight:8,
  seven:7,
  six:6,
  five:5,
  four:4,
  three:3,
  two:2
};
const discard_pile = [];
const suits =["hearts","dimonds","clubs","spades"];

function first_round(player,shuffled_deck){
    console.log(player)
    player.cards.push(shuffled_deck.pop())
    console.log(shuffled_deck.length)
    console.log(player.cards)

    player.cards.push(shuffled_deck.pop())
    console.log(shuffled_deck.length)
    console.log(player.cards)
    Object.values(dealer).push(shuffled_deck.pop())
    Object.values(dealer).push(shuffled_deck.pop())
    console.log(dealer)
}

function card_shuffle(deckOfCards){
  console.log("Shuffle Startting")
  let temp_key = Object.keys(deckOfCards);
  let temp_val = Object.values(deckOfCards)
  let temp_arr = [];
  let shuffle_arr = [];
  let temp_shuffle = '';
  let temp_len;

  for(let cards = 0; cards < Object.keys(deckOfCards).length; cards++){

    for(let ct = 0; ct < temp_val[cards];ct++){
        temp_arr.push(`${temp_key[cards]} of ${suits[ct]}`)
       
    }
  }
  temp_len = temp_arr.length;
  for(let shuffle_ct =0; shuffle_ct<temp_len;shuffle_ct++){
      temp_shuffle = '';
      random_num =Math.floor(Math.random()*temp_arr.length);
      temp_shuffle = temp_arr[random_num];
      delete temp_arr[random_num]
      shuffle_arr.push(temp_shuffle);
  }
  console.log(shuffle_arr.length)
  console.log(shuffle_arr)
  return shuffle_arr;
}

function black_jack(){
  let shuffled_cards=card_shuffle(deckOfCards);
  let first_round_bool = true;
  let continue_game = true;
  let discard_pile = [];
  let player_value = 0;
  let money;
  let continue_sel;

  while(continue_game === true){
      if(money < 0){
          continue_sel=prompt("Sorry You Went Bust!\n Try Again?\n Yes/No:");
          if(continue_sel.toLowerCase === "no"){
              continue_game = false;
          }
          else{
              black_jack();
          }
      }else{
        if(first_round_bool === true){
            const player = new Player(cards=[],money_bet =0,roulette_bet='',money=200,card_value=0);
            first_round(player, shuffled_cards);
            first_round_bool = false;
        }
      }

  }
}


// menu functions

function choice_handler(input_sel){

    if(input_sel ==="1" || input_sel.toLowerCase === "black jack"){
      black_jack();
    }else if(input_sel === "2" || input_sel.toLowerCase === "slot machine"){
      alert("slot machine")
    }else if(input_sel === "3" || input_sel.toLowerCase === "roulette"){
      DeviceAcceleration("roulette")
    }
    else{
      alert("Im Sorry That's Not A Game We Have, Please Try Again.")
      let try_again = prompt(
          "Casino Royal Game List\n"+
          "Pleas Enter The Gamer Number Or Game Name To Select Game!\n"+
          "\n"+
          "*1.Black Jack*\n"+
          "*2.Slot Machine*\n"+
          "*3.Roulette*\n"+
          "***************************\n"+
          "What Game Would You Like To Play?")
      choice_handler(try_again);
    }
  }
  
  
  function intro(){
      alert("Welcome to Casino Royal\nThis is a test game made by Andrew Knorr");
      return true
  }

  
  
function menu(){
      let quit = false;
      let intro_ran = false;
  
      while(quit === false){
          if (intro_ran === false){
            intro_ran=intro();
          }
  
        let menu_option = prompt(
          "Casino Royal Game List\n"+
          "Pleas Enter The Gamer Number Or\n Game Name To Select Game"+
          "*1.Black Jack*\n"+
          "*2.Slot Machine*\n"+
          "*3.Roulette*\n"+
          "***************************\n"+
          "What Game Would You Like To Play?")
        console.log(menu_option)
        choice_handler(menu_option);
        quit = true;
      }
}


  